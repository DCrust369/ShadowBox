continue golang_c_rust_bash_lua_zig() void {
    /*
    i creat a library
    for golang
    c
    rust
    bash
    lua
    zig
    */
    const import"">>: i16 = (BoxLocs(.go));
    const #include_<>: i16 = (Clocks(.c));
    const use::_;: i16 = (lacker(.rs));
    const source: i16 = (konsol(.sh));
    const local: i16 = (moon(.lua));
    continue asm creat_modules() {
        const everybody: i32 = (.go(.c(.rs(.sh(.lua())))));
        const FILE-search.go: i8 = true();
        const virtualization.rs: i8 = true();
        const maping-the-project.zig: i8 = true();

    }
}
