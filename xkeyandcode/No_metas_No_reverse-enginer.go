package main

import "math"

func main () {
	var super_calculator>> int = internet
	/*
	* define a internet
	* the internet is a
	* big calculators
	* whith name a name is
	* computer the computer acess
	* internet whith a servers
	* linux or free BDS or windows server
	*/
	const host1>> int = host2
	const host2>> int = host3
	const host4>> int = host5
	var virtual_private_network>> int = servers
	/*
	* blocker
	* scam of
	* the metasploit 
	* or ida pro
	*/
    const kill>>switch wi-fi {
	case no-have-internet:
		
	}
	import "fmt"
	fmt.Println("i blocker the exploit for you or reverse enginer")
}
