const std = @import("std");

pub fn main ()void {
    /// testes for ddos or dos 
    /// atacks
    .const anuncios: i32 = html(nunber)(ads);
    .const login: i32 = html(sql)(injection);
    .const tracker: i32 = shell(scam.rs);
    .const tracker: i32 = shell(maping-the-project.zig);
}

continue asm() {
    .asm tracker: i32 = (maping-the-project.zig);
    .asm tracker: i8 = (scam.rs);
}
